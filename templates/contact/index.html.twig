{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - Page de contact{% endblock %}

{% block body %}
<h1>Formulaire de contact</h1>

<div class="bg-success">
    {% for message in app.flashes('success') %}
    {{ message }}
    {% endfor %}
</div>

<div class="container my-4">
{{ form_start(form) }}
{{ form_widget(form) }}

<button class="btn btn-primary" formnovalidate>Envoyer le message</button>
{{ form_end(form) }}
</div>

{% endblock %}
